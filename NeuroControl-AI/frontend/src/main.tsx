// Simple React entry point - will work once dependencies are installed
const root = document.getElementById('root');
if (root) {
  root.innerHTML = '<div style="padding: 20px; font-family: Arial, sans-serif;"><h1>🧠 NeuroControl AI</h1><p>Industrial Control System Loading...</p><p>Dependencies installing...</p></div>';
}
